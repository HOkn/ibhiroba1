= render 'questions/header'

.row
  .col-md-1
  .col-md-8
    / CONTENT
    .content_wrap
      .content
        .space10px
        .space10px
        %h4 回答を編集
        = form_for :answer, url: question_answer_path, method: :put do |f|
          // / - if @article.errors.any?
          // /   %error_explanation
          // /     %h2
          // /       = pluralize(@article.errors.count, "error") prohibited
          // /       this article from being saved:
          // /     %ul
          // /       - @answer.errors.full_messages.each do |msg|
          // /         %li = msg
          %p.answerbox
            = f.text_area :content
          %p
            .box_button
              = f.submit '編集を保存', :class => "a btn btn-default"

  .sidebar.col-md-3
    = render 'questions/sidebar'

= render 'questions/footer'
